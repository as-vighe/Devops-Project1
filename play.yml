- hosts: localhost
  name: masterplay
  become: true
  tasks:
   - name: running master script
     script: master.sh
- hosts: slaveservers
  name: slaveplay
  become: true
  tasks:
   - name: running slave script
     script: slave123.sh         
       
   - name: copy docker file
     copy:
       src: "Dockerfile"
       dest: "/home/ubuntu/Devops-Project1"
